import Form from './Form/schema';
import XButton from './XButton/schema';
import RichText from './RichText/schema';

const basicSchema = {
  Form,
  XButton,
  RichText,
};
export default basicSchema;
